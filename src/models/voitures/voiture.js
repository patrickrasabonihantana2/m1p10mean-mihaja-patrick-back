class Voiture {
  constructor(user_id, matricule, marque, modele) {
    this.user_id = user_id;
    this.matricule = matricule;
    this.marque = marque;
    this.modele = modele;
  }
}

module.exports = SessionReparation;
