class Reparation {
  constructor(session_reparation_id, description, montant) {
    this.session_reparation_id = session_reparation_id;
    this.description = description;
    this.montant = montant;
  }
}

module.exports = Reparation;
